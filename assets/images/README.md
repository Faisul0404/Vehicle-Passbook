# Image assets

Place vehicle photographs and branded artwork in this directory. The current Figma Make reference did not expose downloadable image assets, so the Flutter UI intentionally uses Material vehicle icons rather than substitute imagery.